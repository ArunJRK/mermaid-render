{
  "name": "mermaid-render",
  "private": true,
  "scripts": {
    "lint": "pnpm --filter @mermaid-render/core lint",
    "typecheck": "pnpm --filter @mermaid-render/core typecheck",
    "test": "pnpm --filter @mermaid-render/core test",
    "test:browser": "pnpm --filter @mermaid-render/core test:browser",
    "build": "pnpm --filter @mermaid-render/core build",
    "build:demo": "pnpm --filter @mermaid-render/core build:demo",
    "verify:core": "pnpm lint && pnpm typecheck && pnpm test && pnpm test:browser && pnpm build && pnpm build:demo && pnpm --filter @mermaid-render/core test:browser:static-demo && pnpm --filter @mermaid-render/core bundle:check && pnpm --filter @mermaid-render/core pack:check"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
